import KodiWS from '../../src/kodi-ws'

describe('Connection', () => {
  describe('#someMethod', () => {
    it('does something useful', done => {
      expect(false).to.equal(true)
      done()
    })
  })
})
